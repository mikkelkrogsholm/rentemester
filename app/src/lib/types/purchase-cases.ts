export type PurchaseCaseGroupMember = { caseId:string; version:number; sourceFingerprint:string; documentationOutcome:string; accountingProgress:string; vatEvidence:{status:string;reference?:number} };
export type PurchaseNeedGroup = { need:{key:string;question:string}; caseCount:number; members:PurchaseCaseGroupMember[]; selectionHash:string };
export type PurchaseCase = { caseId:string; version:number; source:{kind:"document"|"bank_transaction"|"payable";id:number}; sourceFingerprint:string; documentationOutcome:"unresolved"|"ordinary_evidence_sufficient"|"alternative_evidence_assessed"; accountingProgress:"unposted"|"posted"; canonicalBooking?:{kind:"journal";id:number}; vatEvidence:{status:"not_applicable"|"pending"|"passed"|"failed";reference?:number}; note:string; eventHash:string; createdAt:string; sourceStatus?:{status:"current"|"stale"|"missing";currentSourceFingerprint:string|null} };
export type PurchaseOverview = { scope:{from:string;to:string}; basis:{canonical:{sourceCaseCount:number;postedCaseCount:number;unpostedCaseCount:number;financialAggregation:string;economicEffect:{expense:number;result:number;basis:"posted_ledger"}};provisional:{included:boolean;caseCount:number;unresolvedDocumentationCount:number;alternativeEvidenceCount:number;financialAggregation:string;economicEffect:{status:string;expense:number;expectedVat:number;effects:Array<{caseId:string;status:string;reason?:string;draftId?:string}>}};difference:{provisionalCaseCount:number;canonicalPostingIsUnchanged:boolean}};groups:PurchaseNeedGroup[];sourceHash:string };
