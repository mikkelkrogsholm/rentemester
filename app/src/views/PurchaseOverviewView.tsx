import { useState, type FormEvent } from "react";
import { useParams } from "react-router-dom";
import { api } from "../lib/api";
import { useAsync } from "../lib/useAsync";
import type { PurchaseNeedGroup, PurchaseOverview } from "../lib/types";
import { ErrorState, Loading } from "../components/Feedback";
import { ConfirmDialog } from "../components/ConfirmDialog";

const today=()=>new Date().toISOString().slice(0,10);
const yearStart=()=>`${new Date().getUTCFullYear()}-01-01`;
export function PurchaseOverviewView(){
  const {slug=""}=useParams(); const [from,setFrom]=useState(yearStart); const [to,setTo]=useState(today); const [selected,setSelected]=useState<PurchaseNeedGroup|null>(null); const [sourceKind,setSourceKind]=useState<"document"|"bank_transaction"|"payable">("document"); const [sourceId,setSourceId]=useState(""); const [createPending,setCreatePending]=useState(false); const [actionError,setActionError]=useState<string|null>(null);
  const state=useAsync<PurchaseOverview>(()=>api.purchaseOverview(slug,{from,to}),[slug,from,to]);
  const policy=useAsync(()=>api.accountingApprovalPolicy(slug),[slug]);
  if(state.loading&&!state.data)return <Loading label="Henter købsoversigt…"/>;
  if(state.error||policy.error)return <ErrorState message={state.error??policy.error??"Købsoverblik kunne ikke hentes."} onRetry={()=>{state.reload();policy.reload();}}/>;
  const overview=state.data!;
  function create(event:FormEvent){event.preventDefault();if(!Number.isInteger(Number(sourceId))||Number(sourceId)<=0){setActionError("Kilde-id skal være et positivt heltal.");return;}setCreatePending(true);}
  async function confirmCreate(){try{await api.createPurchaseCase(slug,{source:{kind:sourceKind,id:Number(sourceId)},documentationOutcome:"unresolved",idempotencyKey:crypto.randomUUID()});setSourceId("");setCreatePending(false);state.reload();}catch(cause){setActionError(cause instanceof Error?cause.message:"Købscasen kunne ikke oprettes.");throw cause;}}
  return <section className="statement"><div className="page-head"><div><h2>Købsoverblik</h2><p className="muted">Kildebaseret status og dokumenterede behov. Beløb summeres ikke på tværs af kildetyper.</p></div></div>{actionError&&<div className="card archived-notice" role="alert"><p>{actionError}</p></div>}<div className="row-actions"><label>Fra <input type="date" value={from} onChange={event=>setFrom(event.target.value)}/></label><label>Til <input type="date" value={to} onChange={event=>setTo(event.target.value)}/></label></div><form className="card" onSubmit={create}><h3>Åbn foreløbig købscase</h3><p className="muted">Knytter én eksisterende kilde til en arbejds-case. Den bogfører ikke og ændrer ikke momsstatus.</p><div className="row-actions"><label>Kildetype <select value={sourceKind} onChange={event=>setSourceKind(event.target.value as typeof sourceKind)}><option value="document">Bilag</option><option value="bank_transaction">Bankpost</option><option value="payable">Leverandørfaktura</option></select></label><label>Kilde-id <input required inputMode="numeric" value={sourceId} onChange={event=>setSourceId(event.target.value)}/></label><button className="btn" type="submit">Åbn case</button></div></form><div className="cards"><article className="card"><strong>{overview.basis.canonical.postedCaseCount}</strong><span>Bogførte cases</span></article><article className="card"><strong>{overview.basis.canonical.unpostedCaseCount}</strong><span>Ikke-bogførte cases</span></article><article className="card"><strong>{overview.basis.provisional.unresolvedDocumentationCount}</strong><span>Uafklaret dokumentation</span></article></div><div className="card statement-card"><h3>Grupperede behov</h3>{overview.groups.length===0?<p className="muted">Ingen åbne grupper i perioden.</p>:<ul className="plain-list">{overview.groups.map(group=><li key={group.selectionHash}><strong>{group.need.question}</strong><p className="muted">{group.caseCount} eksakte cases · {group.need.key}</p>{group.need.key==="documentation:unresolved"&&<button type="button" className="btn secondary" onClick={()=>setSelected(group)}>Review den valgte gruppe</button>}</li>)}</ul>}</div>{createPending&&<ConfirmDialog title="Åbn foreløbig købscase" body={<p>Casen bindes til den valgte kilde. Den bogfører ikke og godkender ikke moms.</p>} confirmLabel="Åbn case" onConfirm={confirmCreate} onClose={()=>setCreatePending(false)}/>} {selected&&<ConfirmDialog title="Review købsdokumentation" body={<p>Review kun de {selected.caseCount} præcist viste cases. Handlingen bogfører ikke og ændrer ikke momsstatus.</p>} confirmLabel="Bekræft review" confirmKind="primary" onConfirm={async note=>{await api.reviewPurchaseCaseGroup(slug,{groupId:`ui-group-${crypto.randomUUID()}`,members:selected.members.map(member=>({caseId:member.caseId,expectedVersion:member.version,expectedSourceFingerprint:member.sourceFingerprint})),expectedPolicyEventHash:policy.data?.eventHash,documentationOutcome:"ordinary_evidence_sufficient",note,idempotencyKey:crypto.randomUUID()});setSelected(null);state.reload();}} onClose={()=>setSelected(null)}/>}</section>;
}
