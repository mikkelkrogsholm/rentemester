node_modules/
.env
.DS_Store
*.log
companies/
imports/
*.sqlite
*.sqlite-wal
*.sqlite-shm
/tmp/
graphify-out/cache/
graphify-out/.graphify_root
graphify-out/.rebuild.lock
graphify-out/manifest.json
graphify-out/cost.json
graphify-out/GRAPH_REPORT.md
graphify-out/.graphify_labels.json
# graphify run rooted at the app/ subproject — generated artifact, never committed
app/graphify-out/

# Cockpit SPA (#171) — build output + installed deps
app/node_modules/
app/dist/

# Claude Code: track shared settings.json, ignore local state and agent worktrees
.claude/*
!.claude/settings.json

# Local scratch — private migration data, kept out of git
helheim/

# Private noter (møder, samtaler, research) — ikke i git
notes/
